{% extends 'base.html.twig' %}

{% block title %}Edit Profile{% endblock %}

{% block body %}
    <div class="flex flex-col justify-evenly w-[30rem] h-screen m-auto">
        <h1 class="self-center text-4xl font-bold">Modifier Profil</h1>

        {{ 
            include('profile/_form.html.twig', {'button_label': 'MODIFIER'}) 
        }}

        <a href="{{ path('profile') }}" class="self-center hover:text-blue-900">Retourner au profil</a>
    </div>
{% endblock %}
