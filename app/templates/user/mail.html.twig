<h1>Bonjour,</h1>

<p>Pour reinitialiser votre mot de passe cliquer sur le lien suivant</p>

<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password'{token: resetToken.token}) }}</a>

<p>Ce lien expire dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData'ResetPasswordBundle')|replace({'hour': 'heure'}) }}.</p>

<p>Au revoir !</p>